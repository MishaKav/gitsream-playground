# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  ask_ai:
    if:
      - {{ pr.labels.includes("askai") }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            {{ repo | testingRequest if pr.labels.includes("askai") }}