# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  pr_approved:
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: |
            Hello true {{ "" | getTimestamp() }}

  pr_approved_2:
    if:
      - false
    run:
      - action: add-comment@v1
        args:
          comment: |
            Hello false {{ "" | getTimestamp() }}
